{{#if show}}
  {{#paper-list classNames="list-demo"}}
    {{#each messages as |item|}}
      {{#if item.text }}
        {{component item.component item=item}}
      {{/if}}
    {{/each}}
  {{/paper-list}}
{{/if}}
